profile: production
use_default_rules: true

exclude_paths:
  - .github/
  - ansible/inventories/home/group_vars/

skip_list:
  - role-name
  - var-naming

warn_list:
  - yaml[empty-lines]
  - yaml[colons]
  - jinja[spacing]
